---
agent: agent
---
i would like you to add an additional project under /src that is an mcp server 
- using the official C# mcp nuget package https://www.nuget.org/packages/ModelContextProtocol 
- that uses stdio as the transport
- that implements a single tool that is similar in functionality as the `review subcommand in the gapir cli  tool (see class src\gapir\Handlers\ReviewCommandHandler.cs)